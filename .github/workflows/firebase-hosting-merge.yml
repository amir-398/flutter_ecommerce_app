# Legacy workflow - Remplacé par blue-green-deploy.yml
# Ce workflow est conservé pour compatibilité mais ne devrait plus être utilisé
# Utilisez plutôt le workflow "Blue-Green Deployment" pour les déploiements

name: Legacy Deploy to Firebase Hosting (DEPRECATED)
on:
  push:
    branches:
      - main

jobs:
  deprecation-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Notice
        run: |
          echo "⚠️  WARNING: This workflow is deprecated!"
          echo "📋 Please use the 'Blue-Green Deployment' workflow instead."
          echo "🔗 Go to Actions > Blue-Green Deployment for proper deployment."
          echo ""
          echo "This workflow will be removed in a future update."
          echo "The Blue-Green strategy provides better reliability and rollback capabilities."
          
  # Garder l'ancien workflow pour compatibilité mais le désactiver
  legacy_deploy:
    needs: deprecation-notice
    runs-on: ubuntu-latest
    if: false  # Désactivé par défaut
    steps:
      - name: Legacy deployment (disabled)
        run: echo "This step is disabled. Use Blue-Green Deployment instead."

